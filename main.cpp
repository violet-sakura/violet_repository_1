#include <iostream>

using namespace std;

void violet-sort(int* arr, const int len);

int main(void)
{
	int arr1[] = { 11, 14, 5, 22, 15, 6 };
	int len1 = sizeof(arr1) / sizeof(arr1[0]);
	
	violet-sort(arr1, len1);


	return 0;
}